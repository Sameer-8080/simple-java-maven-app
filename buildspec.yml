version: 0.2
phases:
  install:
    commands:
      - sudo apt update 
      - sudo apt install openjdk-21-jre-headless -y
  pre_build:
    commands:
      - mvn clean
  build:
    commands:
      - mvn package
  post_build:
    commands:
      - mvn clean
